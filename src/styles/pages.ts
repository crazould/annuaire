import { css } from "@emotion/react";

export const pageStyle = css`
  padding-inline: 1rem;
  margin: auto;
  max-width: 1024px;
`;

export const wrapperStyle = css`
  padding-block: 2rem;
  min-height: 90vh;
`;
